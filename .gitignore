# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/

# Xcode / Swift
xcuserdata/
*.xcuserstate
DerivedData/
.build/
*.xcworkspace/xcuserdata/

# macOS
.DS_Store

# NOTE: ios/Barry.xcodeproj is generated by XcodeGen but is committed (NOT ignored)
# so Xcode Cloud can build it directly. Re-run `xcodegen generate` and commit the
# result after editing project.yml or adding/renaming source files.
# (User-specific state inside it is still ignored below.)
ios/Barry.xcodeproj/xcuserdata/
ios/Barry.xcodeproj/project.xcworkspace/xcuserdata/

# Secrets / env
.env

# Art / design source files (not needed for the build)
art/
backend/backtest/data/
